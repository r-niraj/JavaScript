var element;
// element = document; // Returns HTML of document
// element = document.all; // Returns array of tags
// element = document.all[11]; // Selects the element using index
// element = document.head; // Selects head
// element = document.title; // Selects title
// element = document.body; // Selects body
// element = document.links; // Selects links
// element = document.links[0]; // Selects 0th links
// element = document.links[2]; // Selects 4th links
// element = document.images; // Selects Images
// element = document.images[0]; // Selects 0th Images
// element = document.images[1]; // Selects 1st Images
// element = document.forms; // returns the array of forms
// element = document.doctype; // returns doctype
// element = document.URL // Returns URL
// element = document.domain // Returns Domains
// element = document.baseURI // Returns Base URI
// element = document.getElementById('wrapper') // Selects ID Wrapper
// element = document.getElementById('footer') // Selects ID Footer
// element = document.getElementsByClassName('abc') // Selects elements by class Name abc
// element = document.getElementsByClassName('abc')[0] // Selects 0th elements by class Name abc
// element = document.getElementsByTagName('h2') // Selects elements by tag name h2
// element = document.getElementsByTagName('ul') // Selects elements by tag name ul
element = document.getElementsByTagName('ul')[0] // Selects 0th elements by tag name ul

console.log(element);
